apiVersion: uds.dev/v1alpha1
kind: Package
metadata:
  name: trust-manager
  namespace: {{ .Release.Namespace }}
spec:
  network:
    allow:
      - direction: Ingress
        remoteGenerated: IntraNamespace

      - direction: Egress
        remoteGenerated: IntraNamespace
